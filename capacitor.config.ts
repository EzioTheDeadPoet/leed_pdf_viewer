import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yourcompany.leedpdf',
  appName: 'Leed PDF Viewer',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
